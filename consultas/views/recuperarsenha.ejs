<!DOCTYPE html>
<html lang="pt-BR">

  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Rose - Esqueceu a Senha</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet">
    <!-- Fonte josh -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Jost:ital,wght@0,100..900;1,100..900&display=swap"
      rel="stylesheet">

    <!-- Link com CSS -->
    <link rel="stylesheet" href="/styles/paginaesqueceuasenha/esqueceuasenha.css">
  </head>

  <body>
    <!-- header: onde vai ter o botao para voltar -->
    <header class="p-4">
      <button onclick="history.back()" class="btn btn-primary"><i class="bi bi-arrow-left text-black"></i></button>
    </header>

    <!-- main: onde vai ter a parte de recuperação de senha -->
    <main>
      <div class="black"></div>
      <div class="container d-flex justify-content-center align-items-center div-esqueceu">
        <div class="fundo">
          <div class="parte-branca">
            <div class="card-body esqueceu-senha d-flex justify-content-center align-items-center">
              <h3 class="text-center mb-4">Esqueceu a Senha</h3>

              <!-- Formulário para solicitar token -->
              <form id="forgotPasswordForm">
                <div class="form-group">
                  <label for="email">Seu Email</label>
                  <input type="email" class="form-control" id="email" placeholder="Insira seu email aqui" required>
                  <small id="emailError" class="form-text text-danger d-none">Por favor, insira um email
                    válido.</small>
                </div>
                <button type="submit" class="custom-button">Obter Token</button>
              </form>

              <!-- Formulário para inserir o token e nova senha -->
              <form id="resetPasswordForm" onsubmit="return redefinirSenha()" class="d-none">
                <div class="form-group mt-4">
                  <label for="token">Token</label>
                  <input type="text" class="form-control" id="token" placeholder="Insira o token recebido" required>
                  <small id="tokenError" class="form-text text-danger d-none">Token inválido.</small>
                </div>
                <div class="form-group">
                  <label for="newPassword">Nova Senha</label>
                  <input type="password" class="form-control" id="newPassword" placeholder="Insira a nova senha"
                    required minlength="6">
                  <small id="newPasswordError" class="form-text text-danger d-none">A senha deve ter pelo
                    menos 6 caracteres.</small>
                </div>
                <button type="submit" class="custom-button">Redefinir Senha</button>
              </form>

              <div class="mt-3 text-center">
                <p>Lembrou a senha? <a href="/login">Entrar</a></p>
              </div>
            </div>
          </div>
        </div>
      </div>
    </main>
    <script src="/javascript/recuperarsenha.js"></script>

    <!-- Script para lidar com envio do token e redefinição de senha -->
    <script src="/consultas/public/javascript/jsPaginaEsqueceuASenha/esqueceuasenha.js"></script>
  </body>

</html>